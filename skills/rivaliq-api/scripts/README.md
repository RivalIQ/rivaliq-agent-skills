# Scripts

This directory is reserved for public helper scripts that may accompany the
`rivaliq-api` skill.

If added later, scripts should be:

- safe for public distribution
- client-neutral where possible
- useful to agents or users installing the skill

It is intentionally empty in the initial repo setup.
